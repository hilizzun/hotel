<Window x:Class="WpfApp1.CleanerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="CleanerWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Мой график уборок"
           FontSize="24"
           FontWeight="Bold"
           HorizontalAlignment="Center" 
           Margin=" 10"/>
        <StackPanel Grid.Row="1">
            <ComboBox Name="Room"  Width="200" Margin="10"/>
            <Button Content="Завершить уборку" Name="FinishButton" Click="FinishButton_Click" Height="25" Width="200" Margin="10,0,10,10"/>
        </StackPanel>
        <DataGrid Name="CleansDataGrid" AutoGenerateColumns="False" 
          Grid.Row="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата уборки" Binding="{Binding cleaning_date}" Width="200" />
                <DataGridTextColumn Header="Комната" Binding="{Binding room}" Width="200" />
                <DataGridTextColumn Header="Статус" Binding="{Binding status}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
