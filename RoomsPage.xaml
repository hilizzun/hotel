<Page x:Class="WpfApp1.RoomsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RoomsPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Вывести статистику по загруженным номерам с: " VerticalAlignment="Center"/>
            <DatePicker Margin ="10" Name ="StartDate" Width="150"/>
            <TextBlock Text="по: " VerticalAlignment="Center"/>
            <DatePicker Margin ="10" Name ="FinishDate" Width="150"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" Grid.Row="1">
            <TextBlock Name="VIP" Text="VIP: " VerticalAlignment="Center" Margin="10"/>
            <TextBlock Name="Standart" Text="Standart: " VerticalAlignment="Center" Margin="10"/>
            <TextBlock Name="Econom" Text="Econom: " VerticalAlignment="Center" Margin="10"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" Grid.Row="2">
            <Button Name="RefreshButton" Content="Обновить" Padding="10,5" Margin="0 0 10 0"
                    Click="RefreshButton_Click"/>
            <TextBlock Name="StatisticsTextBlock" Text="Статистика: "
                       VerticalAlignment="Center"/>
            
        </StackPanel>

        <DataGrid Name="RoomsDataGrid" AutoGenerateColumns="False" 
                  Grid.Row="3">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="70" />
                <DataGridTextColumn Header="Этаж" Binding="{Binding floor}" Width="130" />
                <DataGridTextColumn Header="Номер" Binding="{Binding number}" Width="130" />
                <DataGridTextColumn Header="Категория" Binding="{Binding category}" Width="150" />
                <DataGridTextColumn Header="Статус" Binding="{Binding status}" Width="150" />
                <DataGridTextColumn Header="Стоимость за ночь" Binding="{Binding price_per_night}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
