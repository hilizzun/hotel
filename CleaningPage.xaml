<Page x:Class="WpfApp1.CleaningPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CleaningPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Name="StaffTextBlock" Text="Сотрудник: "
               VerticalAlignment="Center"/>
            <ComboBox Name="Staff"  Width="200" Margin="10"/>
            <TextBlock Name="RoomTextBlock" Text="Комната: "
               VerticalAlignment="Center"/>
            <ComboBox Name="Room" Width="200"  Margin="10"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            <TextBlock Name="DateTextBlock" Text="Дата уборки: "
               VerticalAlignment="Center"/>
            <DatePicker Name="CleaningDate"  Width="200" Margin="10"/>
            <Button Content="Назначить уборку" Name="CleaningButton" Click="CleaningButton_Click" Height="25" Margin="10"/>
        </StackPanel>
        <DataGrid Name="CleaningDataGrid" AutoGenerateColumns="False" 
          Grid.Row="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата уборки" Binding="{Binding cleaning_date}" Width="150" />
                <DataGridTextColumn Header="Комната" Binding="{Binding room}" Width="100" />
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding cleaner}" Width="200" />
                <DataGridTextColumn Header="Статус" Binding="{Binding status}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
