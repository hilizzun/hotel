<Page x:Class="WpfApp1.GuestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GuestsPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button Name="UpdateButton" Content="Обновить" Padding="10,5" Margin="0 0 10 0"
            Click="UpdateButton_Click"/>
        </StackPanel>
        <DataGrid Name="GuestsDataGrid" AutoGenerateColumns="False" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding first_name}" Width="150" />
                <DataGridTextColumn Header="Фамилия" Binding="{Binding last_name}" Width="150" />
                <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="150" />
                <DataGridTextColumn Header="Телефон" Binding="{Binding phone}" Width="150" />
                <DataGridTextColumn Header="Номер документа" Binding="{Binding document_number}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Page>
